{
  "name": "@openboard/aurora-protocol",
  "version": "0.1.0",
  "description": "Aurora LED wire protocol codec for the open_board_leds controller",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "smoke": "npm run build && node tools/frames.mjs > /tmp/openboard-show.json && uv run --quiet --with bleak python3 tools/send.py /tmp/openboard-show.json",
    "frames": "node tools/frames.mjs"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
